# Contributing

1. Fork the repository and create a focused branch.
2. Run `npm ci`, then `npm run check` before opening a pull request.
3. Do not include real Unraid templates, Docker socket data, private URLs, or credentials in issues, fixtures, or screenshots.
4. Keep changes compatible with Node.js 22 and both amd64 and arm64 Docker builds.
